public class PickByHistory implements Strategy {
    private final List< Drawing > history;
    private List< Integer > pick;
    public PickByHistory ( final List < Drawing >  history, final int numTickets ) {
	this.history = history;
	this.pick = initializePick ( numTickets );
	analyzeHistory ( ) 
    }
    
    // Initialize pick 
    private ArrayList< Integer > initializePick ( ) { 
	for ( i = 0; i < Drawing.NUM_BALLS; i++ )
    }
	

}
